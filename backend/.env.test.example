# Points integration tests at a separate database, so they never touch
# your real dev data. Same Postgres instance, different database name.
NODE_ENV=test
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/life_tracker_test?schema=public"
